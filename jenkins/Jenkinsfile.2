pipeline {
    agent { docker 'jlrigau/maven-git' } 
    stages {
        stage('Example Build') {
            steps {
                 sh 'mvn --version'
                sh 'java -version'
                sh 'git --version'
                sh 'python --version'
            }
        }
    }
}
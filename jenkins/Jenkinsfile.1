pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                sh 'pwd'
                sh './deploy hello'
                touch file: 'test.txt'
                sh 'ls -al'
                script {
                    def model = readMavenPom
                    println model

                }
                
            }
        }
    }
}
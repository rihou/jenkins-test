pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                sh 'pwd'
                sh './deploy hello'
                touch file: 'test.txt'
                sh 'ls -al'
                script {
                    def model = readMavenPom file: 'jenkins/pom.xml'
                     
                    def version = model.getVersion()
                    env.version = version
                    println version

                    println "hello"

                }
                sh 'echo ${env.version}'
                
            }
        }
    }
}